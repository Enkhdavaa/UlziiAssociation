---
import MainLayout from '../../layouts/MainLayout.astro'
---
<MainLayout>
    <div class="support-container">
        <div class="support-header">
            <h1 class="support-title">Support Our Mission</h1>
            <p class="support-subtitle">
                Every book matters. Help us bring more stories and learning opportunities to children in our community.
            </p>
        </div>

        <div class="card-container">
            <a href="/support/donate-books" class="action-card">
                <div class="card-icon donate-icon">
                    <svg width="28" height="28" aria-hidden="true"><use href="/svgs.svg#book-donate"/></svg>
                </div>
                <div class="card-body">
                    <h2>Donate Books</h2>
                    <p>Have books you no longer need? Pass them on and give them a new life in the hands of a child.</p>
                </div>
                <span class="card-arrow">→</span>
            </a>

            <a href="/books" class="action-card">
                <div class="card-icon library-icon">
                    <svg width="28" height="28" aria-hidden="true"><use href="/svgs.svg#book-library"/></svg>
                </div>
                <div class="card-body">
                    <h2>Book Library</h2>
                    <p>Browse our collection of donated books and request one for free — no cost, just good reading.</p>
                </div>
                <span class="card-arrow">→</span>
            </a>
        </div>
    </div>
</MainLayout>

<style>
    .support-container {
        max-width: 860px;
        margin: 0 auto;
        padding: 3rem 1.5rem;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 3rem;
    }

    .support-header {
        text-align: center;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .support-title {
        font-size: var(--fs-700);
        font-weight: var(--fw-bold);
        color: var(--primary-blue);
    }

    .support-subtitle {
        font-size: var(--fs-500);
        color: var(--color-muted);
        max-width: 520px;
        line-height: 1.6;
    }

    .card-container {
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
        width: 100%;
    }

    @media (min-width: 640px) {
        .card-container { flex-direction: row; }
    }

    .action-card {
        flex: 1;
        display: flex;
        align-items: center;
        gap: 1.25rem;
        padding: 1.75rem;
        background: #fff;
        border: 1px solid var(--color-border);
        border-radius: var(--radius-lg);
        box-shadow: var(--shadow-md);
        text-decoration: none;
        color: inherit;
        transition: box-shadow 0.2s ease, transform 0.2s ease;
    }

    .action-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .card-icon {
        flex-shrink: 0;
        width: 56px;
        height: 56px;
        border-radius: var(--radius-lg);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .donate-icon  { background: #fff7ed; color: var(--primary-orange); }
    .library-icon { background: #eff6ff; color: var(--primary-blue); }

    .card-body { flex: 1; }

    .card-body h2 {
        font-size: var(--fs-500);
        font-weight: var(--fw-bold);
        color: #111827;
        margin-bottom: 0.3rem;
    }

    .card-body p {
        font-size: var(--fs-400);
        color: var(--color-muted);
        line-height: 1.5;
    }

    .card-arrow {
        font-size: 1.2rem;
        color: #9ca3af;
        flex-shrink: 0;
        transition: transform 0.2s ease, color 0.2s ease;
    }

    .action-card:hover .card-arrow {
        transform: translateX(4px);
        color: var(--primary-blue);
    }
</style>
